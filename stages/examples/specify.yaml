name: Specify
order: 0
skill: fabrik-specify
comment_skill: fabrik-specify-comment
auto_advance: false
model: sonnet
max_turns: 50
read_only: false
allowed_tools:
  - Read
  - Grep
  - Glob
  - Write
  - Edit
  - TodoWrite
  - WebSearch
  - WebFetch
  # Prefix wildcards, not exact strings: `Bash(git add)` matches only the literal
  # `git add` with no arguments. Narrow verbs, but each open at the argument.
  - Bash(git rev-parse:*)
  - Bash(git add:*)
  - Bash(git commit:*)
  - Bash(git diff:*)
  - Bash(gh issue view:*)
  - Bash(find:*)
completion:
  type: claude
